"""Deviatrix Genesis — Idea Foundry with MathExec + SymPy MCP.

A 3-diamond × 3-expedition conductor that uses SymPy MCP as the
mathematical transmission (not a creativity engine) and independent
robust statistics as the telemetry. The verifier alone terminates.

The complete car:
  CHAOS                — turbo (enters improbable regions)
  Collision            — transmission (maps relations between distant spaces)
  Deviate              — steering (forces escape from the median)
  Pattern Recognition  — tires + road model (structural recurrence)
  SymPy MCP            — dyno + transmission controller (proves formulas)
  Robust statistics    — telemetry (measures actual distance from world)
  Independent verifier — brakes + finish-line authority (what survives)

Three expeditions per diamond construct the real transformation:
  construct the extreme, construct its opposite, then author the coherent
  third thing that survives both.
"""

from __future__ import annotations

__version__ = "0.1.0"
__all__ = [
    "schemas",
    "mathexec",
    "sympy_mcp",
    "verifier",
    "diamonds",
    "iqrsqpi",
    "conductors",
]
